apiVersion: v1
kind: Service
metadata:
  name: demo-micro
  labels:
    app: demo-micro
  namespace: reto5
spec:
  selector:
    app: demo-micro
  type: NodePort
  ports:
    - name: http
      port: 9080
      targetPort: 8080
